# 2026-08-07 工作日志

## 编写「票据系统 WAR/JAR 差异比对与智能分析工具」需求文档
- 任务来源：用户要求编写需求，分析 BEMP 5.0（`D:\code\QJ\BEMP5.0DEV`，Maven 多模块：55 pom / 58 jar / 27 war）打出的 war/jar 包，对比新老版本差异。
- 产出文件：`D:\code\otherProjects\18_comparePakage\需求规格说明书-票据系统WAR-JAR差异比对与智能分析工具.md`
- 文档核心要点：
  - 全量差异文件树（新增/删除/修改/未变），对标 BCompare 双栏对照。
  - 内置反编译器（CFR/Procyon/Quiltflower）还原 .class 做内容 diff。
  - 大模型智能分析：改动意图、风险等级、影响范围、测试要点（结构化 JSON 输出）。
  - 导出 Markdown 报告；导出差异 class / 差异 jar / 反编译源码 zip。
  - 所有配置（尤其 AI 服务：Base URL / API Key / 模型，含连接测试）在 UI 配置，API Key 本地加密不落明文。
  - 打包为 exe 双击即用（推荐 JavaFX + 内嵌 JRE + jpackage）。
- 技术选型结论：纯 Java 桌面方案（JavaFX + 内嵌反编译器 + jpackage），最契合"分析 Java 包 + 双击 exe + UI 配一切"。

## 评审 + v1.1 修订（同日）
- 对 v1.0 做评审：1 阻塞级（差异粒度策略、AI 规模成本）+ 4 高优先级（fat jar 路径、金融合规、删除类、非文本边界）+ 6 中优先级。
- 已按 11 点逐项修订为 v1.1：新增 §5.2.1 差异粒度分层(L0/L1/L2+路径命名空间)、§5.5.1 两阶段AI分析(概览→深读)、按包类型路径映射表、§5.9.1 金融合规与审计(本地模型代码不出机+审计日志)、删除类专项汇总、非文本比对边界、版本自动提取、代理配置、§5.10 UI线框、配置覆盖关系、报告默认Top-K体量。
- 文件：`需求规格说明书-票据系统WAR-JAR差异比对与智能分析工具.md`（v1.1）。

## 开发任务清单 WBS（同日，续）
- 用户说"继续"，将 v1.1 需求拆为 WBS：M1(P0+合规, ≈45人天)/M2(P1, ≈17)/M3(P2/P3, ≈10)。
- 36 个任务(T01–T36)，含依赖关系图、关键路径、验收映射(AC1–AC9→任务)、风险注意。
- 文件：`开发任务清单-WBS-票据系统WAR-JAR差异比对与智能分析工具.md`。

## 分层解析引擎原型（同日，续）
- 用户再次"继续"→ 选最高风险项（WBS 风险注记：分层解析先用真实 war 做原型验证），用 Python 实现参考原型验证 L0/L1/L2 分层 + 命名空间 + 内部/第三方识别 + diff。
- 文件：`prototype/diff_engine.py`（含 make-fixture / compare 子命令，纯标准库），生成 `prototype/sample_v1.war`、`sample_v2.war`、`diff_result.json`。
- 运行验证通过：版本自动提取(5.0.1→5.0.2)；L1 展开内部 jar(B.class 改)、第三方 jar 仅 jar 级(third-party.jar 改不展开)、删除/新增类、命名空间无冲突。统计 新增1/删除1/修改5/未变1。
- 说明：本原型是 Java 移植参考；量产由 JavaFX + CFR 反编译承载（本环境无 Java 运行时，反编译集成留待后续）。

## 反编译集成 spike + UI 高保真原型（同日，续）
- 用户"继续完成未完成的任务"→ 发现本环境实际有 **Java 8 (1.8.0_341) + javac + JAVA_HOME**，可真跑反编译，遂把反编译 spike 从"设计"升级为"实测"。
- 下载 CFR 0.152（cfr.jar, 2.16MB）到 `prototype/` 作为生产反编译器参考。
- 编写 `prototype/decompile.py`：可插拔后端（CFR 主 / javap 降级），统一接口 `decompile(class)->{source,engine,ok}`；**关键坑：CFR 在有中文常量的 class 上按系统 GBK 输出 stdout，须按字节捕获并容错解码(utf-8→gbk→latin-1)**。
- 实测：编译 v1/v2 版 InvoiceService.java（javac -encoding UTF-8），CFR 反编译双向成功，产出 `decompiled_src_v1.java`/`decompiled_src_v2.java`/`decompiled_diff.txt`（unified diff 42 行，正确反映金额上限提升、新增 TAX_RATE、issue/validate 签名变更）。这证明 T07/T08（反编译+双栏diff）全链路可行，decompiled_diff.txt 即 UI 双栏与 AI 阶段B 深读的输入。
- 编写 `prototype/ui_prototype.html`：交互式高保真 UI 原型（自包含，直接用浏览器/预览打开）。含：顶部工具栏(老/新包路径+开始比对+导出报告/资产+⚙设置)、左差异文件树(BCompare 配色+ L0/L1/L2 图例+筛选chip)、中双栏源码 diff(用真实 decompiled 数据做 LCS 并排高亮)、右 AI 分析面板(单文件卡片/全局汇总/破坏性变更/审计日志 4 Tab)、配置中心弹窗(AI 厂商预设+BaseURL+APIKey掩码显示+模型+连接测试+HTTP/HTTPS代理、解析/导出/界面配置，全部 UI 入口)。体现 v1.1 全部硬诉求。
- 当前累计交付：需求 v1.1、评审修订、WBS、分层解析原型、反编译 spike、UI 高保真原型（共 6 份）。
- 后续可选：① 把 T05/T07 展开详细设计+接口契约；② 真实 BEMP war 端到端验证（需把 BEMP 某 war 拷入 prototype 跑 diff_engine+decompile）；③ 起草 UI 真值测试/验收脚本。

## 端到端验证（真实包，同日续 · 关闭 WBS "真实 war 验证"风险项）
- 用户第三次"继续"→ 用真实 Java 包做端到端验证（非合成夹具）。
- 本机真实资产：BEMP5.0DEV 含真实 `.war`：`banks/ext-hnnxbank/hnnxbank-adapter-deploy/target/bemp-adapter.war`（版本 5.20230102036M.0.0-SNAPSHOT）；BEMP 业务 class 在 `*/target/classes/com/hundsun/bemp/...`（共 13197 个真实业务 class）。m2 仓库无多版本库、mvn 损坏，但网络可用（CFR 已下）。
- 下载真实双版本库 commons-lang3 3.12.0 / 3.14.0（v1=345 class，v2=404 class）作为真实"两版 diff"样本。
- 增强 `prototype/diff_engine.py`：① 普通 jar 支持 `--expand-all` 把自身 class 全当 L1；② `--decompile` 对 L1 修改类调 decompile.py/CFR 做双栏源码 diff，带 `--top-k` 上限（默认12）防爆炸；③ 新增 `inspect_package()` 单包检视；④ 产出 `real_diff_report.md`（FR7 报告雏形）。
- 编写 `prototype/e2e_real.py` 驱动三类验证，实测结果：
  - **A) 真实两版 jar diff**：commons-lang3 3.12.0→3.14.0，版本自动提取成功；统计 新增63/删除4/修改345/未变1（业务类级变更412）；反编译 12 个修改类（Top-K，跳过329），产出 real_diff.json + real_diff_report.md（含真实源码级 diff）。
  - **B) 真实 BEMP war 解析**：包类型=war，版本=MANIFEST 自动提取；总条目 30877 → **L2=163（第三方 jar 折叠）/ L1=30710（业务 class 展开）/ L0=4**。证明 L0/L1/L2 分层在 3 万级真实 war 上不爆炸、正确区分业务码与第三方依赖。
  - **C) 真实 BEMP 业务 class 反编译**：CFR 反编译 `POBS010001ServieImpl.class`（真实 service impl，含 @CloudComponent、fastjson、hundsun 框架）成功，184 行干净 Java 源码；CFR 对真实企业级字节码（注解/泛型/Spring Cloud）处理正常。
- 结论：解析/分层/版本提取/反编译/源码 diff/Top-K/报告 全链路在真实包上验证通过。
- 已知小瑕疵：`print_tree` 对普通 jar 的 class 仍显示"包级"(L0) 标签（条目内部 layer 实为 L1、反编译正确）；仅展示标签问题，不影响数据与报告。可后续修正。
- 累计交付（8 份）：需求 v1.1 / 评审修订 / WBS / 分层解析原型 / 反编译 spike / UI 高保真原型 / 端到端验证驱动+真实报告+真实BEMP反编译源码。

## 详细设计 + 接口契约 + print_tree 修复（同日续）
- 用户第四次"继续"→ 把已验证原型转化为 Java 量产版可编码接口契约，并修 print_tree bug。
- **修复 print_tree 层级标签 bug**：原实现按路径字符串重推 layer，普通 jar 比对的 class 会误判 L0。改为优先从 `entries[k].layer` 取真实 layer（`diff_engine.py` print_tree 现接受 old_entries/new_entries），已用 commons-lang3 双版本 jar 实测：class 现正确显示[业务]、META-INF 正确保持[包级]。
- 新增 `详细设计-接口契约-票据系统WAR-JAR差异比对与智能分析工具.md`（v1.0）：① 架构分层（app/core/config/model）；② 10 个核心 Java 数据结构（PackageSnapshot/LogicalEntry/DiffResult/DecompiledUnit/AppConfig 等）；③ 7 大模块接口契约（parse/diff/decompile/ai/report/export/audit）含真实方法签名；④ 路径映射表（war/fat_jar/jar）；⑤ 两阶段 AI 数据契约（StageASummary/FileAnalysis + 裁剪/脱敏/成本闸门）；⑥ 错误码与降级矩阵；⑦ 与 WBS T04–T15 逐任务回链；⑧ 已知缺陷清单（含 GBK 容错、本地模型零外发 AC9 硬门槛）。
- 累计交付（9 份）：+ 详细设计-接口契约文档。

## Java 移植端口 core.parse + core.diff（同日续，最高杠杆落地）
- 用户第五次"继续"→ 把设计文档里最设计关键的 `core.parse`+`core.diff`（纯逻辑、T04/T05/T06）移植成**真实可编译 Java**（JDK 8，本机可用；不依赖 JavaFX）。
- 落地产物：`prototype/java_core/src/com/bempdiff/{model,config,parse,diff}/` + `Main.java`（CLI 验证驱动）。共 14 个 .java 文件：model（PackageType/Layer/FileClass/EntrySource/LogicalEntry/PackageSnapshot）、config（ParseConfig）、parse（PackageParser 含 detectType/path_mapping/classify/extractVersion/buildLogicalEntries/readEntryBytes）、diff（DiffResult/DiffStatus/DiffStats/DiffEngine）。
- **关键工程决策（优于原型）**：LogicalEntry 只存 sha256+size+EntrySource 定位，**不缓存整条目字节**，实现详细设计 §5.1 的"流式防 OOM"（3 万级 war 不爆内存）；嵌套 lib 用临时文件打开内嵌 zip（量产版可改内存映射）。
- **真实验证（与 Python 原型逐字节对齐）**：
  - 真实 BEMP war：`type=WAR, version=5.20230102036M.0.0-SNAPSHOT, total=30877, L0=4/L1=30710/L2=163` —— 与 Python 完全一致。
  - 真实 commons-lang3 3.12.0→3.14.0：`added=63 deleted=4 modified=345 unchanged=1, bizChanged=412, jarChanged=0` —— 与 Python 完全一致；层级标签正确（class=[L1]、META-INF=[L0]），print_tree bug 修复已并入端口。
- 编译要点：本机 javac 默认 GBK，源码 UTF-8 必须 `-encoding UTF-8`；`java` 不接受 Git-Bash `/d/` 路径，须传 `D:/` 风格绝对路径。
- 结论：设计→原型→Java 端口三套实现在真实包上行为一致，core.parse/diff 可视为冻结基线，后续仅需在 Java 上加 UI/反编译/AI/报告。
- 累计交付（10 份）：+ Java 移植端口（core.parse+core.diff，可编译可跑）。

## Java 移植 core.decompile + 全链路真实验证（同日续，闭环 T07/T08）
- 用户第六次"继续"→ 在已跑通的 core.parse+core.diff 之上叠加 core.decompile（T07/T08），闭合"解析→差异→反编译→源码diff"全链路，并在真实双版本 jar 上验证。
- 新增 `prototype/java_core/src/com/bempdiff/{model/DecompiledUnit.java, decompile/Decompiler.java}`；扩展 `Main.java` 增加 `decompile` 子命令。
- **Decompiler 实现要点（与 Python prototype 对齐）**：CFR 主（子进程 `java -jar cfr.jar <class> --sugarenums false --hideutf false --silent true`）；失败降级 javap（`-p -c`）；**GBK 容错解码**（utf-8→gbk→latin-1，Java 侧 `new String(bs, enc)` 逐编码尝试）已在真实含中文常量 class 上验证不崩；行级 diff 用自带 LCS（不引第三方）；新增/删除类带提示、删除类标"潜在破坏性变更"。
- **真实验证（真实 cfr.jar 0.152 + 真实 commons-lang3 3.12.0→3.14.0）**：L1 class 候选=408（=341 修改类+63 新增+4 删除，已正确排除 4 个 L0 非 class 条目），反编译=8（topK=8），跳过=400；engine=cfr、OK=true；diff 呈现真实语义改动（如 `getShortClassName` 由 for-loop 改为 stream 写法）。
- 编译踩坑：`new String(bs,"utf-8")` 抛受检 `UnsupportedEncodingException`，兜底分支须 try/catch。
- 结论：解析/分层/差异/反编译/源码diff 在 Java 侧与 Python 原型行为一致，T04–T08 全链路可视为冻结基线；后续仅缺 core.ai（两阶段大模型）、core.report（MD导出）、core.export（差异资产）、JavaFX UI、jpackage exe 打包。
- 累计交付（11 份）：+ Java 移植端口 core.decompile + 全链路验证。

## Java 移植 core.report + 真实报告导出（同日续，闭环 T12/FR7）
- 用户第七次"继续"→ 在已闭环 T04–T08 的 core 之上叠加 core.report（T12，FR7），并导出真实 .md 报告文件。
- 新增 `prototype/java_core/src/com/bempdiff/report/MarkdownReport.java`；扩展 `Main.java` 增加 `report` 子命令（全链路 parse→diff→decompile→report，一次跑完并写出 .md）。
- **报告结构（与需求 v1.1 §FR7 对齐）**：① 差异统计；② 差异文件树（全量清单）；③ 反编译源码级差异（Top-K 完整 diff，默认15）；④ 破坏性变更清单（删除类单列）；⑤ 审计摘要（基础，AI 章节标注待 core.ai 补）。
- **真实验证**：真实 commons-lang3 3.12.0→3.14.0 → 导出 `java_core_report.md`（约 6665 行）：全量清单含 412 差异项 + 15 个真实 CFR 源码 diff（如 AnnotationUtils 重构）+ 删除类清单（4 个）+ 审计摘要。文件 UTF-8 正确、结构完整。
- 结论：core.parse/diff/decompile/report 四模块在 Java 侧与 Python 原型行为一致，可视为 T04–T08+T12 冻结基线。距可交付 exe 还差 core.ai（两阶段大模型）、core.export（差异资产）、JavaFX UI、jpackage 打包。
- 累计交付（12 份）：+ Java 移植端口 core.report + 真实报告文件。

## Java 移植 core.export + 真实资产导出（同日续，闭环 T13/FR6）
- 用户第八次"继续"→ 在已闭环 T04–T08+T12 的 core 之上叠加 core.export（T13，FR6），产出可流转的差异资产。
- 新增 `prototype/java_core/src/com/bempdiff/export/AssetExporter.java`；扩展 `Main.java` 增加 `export` 子命令（全链路 parse→diff→decompile→export，一次跑完并写出导出目录）。
- **AssetExporter 三项导出（与需求 §FR6 / 详细设计 §5.6 对齐）**：① exportDiffClasses（复制 ADDED/DELETED/MODIFIED 的 class 字节，保持命名空间，并附 .STATUS.<name> 状态侧车标记）；② exportDiffJars（变化的 lib/*.jar，L2）；③ exportDecompiledSources（Top-K 完整源码 + MANIFEST.txt 打包 zip）。
- **真实验证**：真实 commons-lang3 3.12.0→3.14.0 → 导出 `java_core_export/`：diff-classes 含 816 条目（412 真实 .class 字节 + 408 状态标记，即 408 个 class 变更侧 ×2），decompiled-sources.zip 含 15 个真实 .java + MANIFEST.txt，diff-jars 为空（plain-jar 比对无嵌套 lib，路径正确；war 输入时会被填充）。抽一个导出 class 用 CFR 回解验证字节真实。
- **已知小瑕疵（非阻塞）**：状态侧车标记文件名仅用末级文件名（不含包路径），同包同名类不同路径时可能碰撞；量产版可改为 sidecar `<name>.status` 或并入 manifest，但真实 class 字节导出无误（命名空间完整）。
- 结论：core.parse/diff/decompile/report/export 五模块在 Java 侧与 Python 原型行为一致，可视为 T04–T08+T12+T13 冻结基线。距可交付 exe 还差 core.ai（两阶段大模型）、JavaFX UI、jpackage 打包。
- 累计交付（13 份）：+ Java 移植端口 core.export + 真实资产导出目录。

## Java 移植 core.ai 两阶段分析 + 离线回放验证（同日续，闭环 T09/T10/T11）
- 用户第九次"继续"→ 在已闭环的 core 之上叠加 core.ai（T09/T10/T11），用离线回放 Mock 验证两阶段流程（不触网、无需 API Key，符合本环境限制）。
- 新增：`config/AiConfig.java`（§5.9 AI 配置模型）、`ai/StageASummary.java`、`ai/FileRisk.java`、`ai/FileAnalysis.java`、`ai/AiAnalyzer.java`（接口+DecompileReq+成本闸门默认方法）、`ai/PromptBuilders.java`（阶段A/B prompt 构造+脱敏）、`ai/MockAiAnalyzer.java`（离线回放实现）；扩展 `Main.java` 增加 `ai` 子命令。
- **两阶段流程（与详细设计 §5.4 对齐）**：buildStageAPrompt（差异清单+每文件截断 diff 摘要，前 stageAFileSampleLines 行、最多 stageATopK 文件）→ stageA 整体风险/影响/测试主题+每文件初评；stageB 仅对 high/medium 或前 stageBTopK 候选发完整 diffText 逐文件深读；成本闸门 estimateTokens>costGateWarnTokens 返回 gateWarn 由 UI 二次确认。
- **脱敏（§5.9.1 金融合规）**：非本地/ollama 模型时正则擦除疑似身份证(18位)/密钥(AK/SK/KEY/SECRET/TOKEN)/手机号；本地模型代码不出机不脱敏。
- **真实验证（真实 commons-lang3 3.12.0→3.14.0，离线回放）**：阶段A 解析回放 JSON/文本 → overallRisk=HIGH、影响=涉及发票开具与校验核心链路、测试主题提取正确；prompt tokens≈19246 > 阈值8000 → 成本闸门触发=true（正确）；阶段B 深读15文件（含1个回放命中+14个兜底）；prompt 落地 replay 目录供人工核对/替换为真实响应。
- 编译踩坑：Main 引用 MockAiAnalyzer 未 import、AiConfig 包路径误写 ai（实为 config）；已修。
- 结论：core.parse/diff/decompile/report/export/ai 六模块在 Java 侧与 Python 原型行为一致，可视为 T04–T13 冻结基线。距可交付 exe 还差 JavaFX UI 三区（需 JDK21）、jpackage 打包、真实 HttpAiAnalyzer（替换 Mock，接口不变）。
- 累计交付（14 份）：+ Java 移植端口 core.ai 两阶段分析 + 离线回放验证。
