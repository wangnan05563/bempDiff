# 2026-08-20 工作日志

## 评审收尾复核：L 级建议核实 + 死代码清理（17:26-17:40）
- **L1（statusOf NPE）核实为理论性、非缺陷**：`DiffResult` 构造函数已为全部 `DiffStatus` 预置空列表，`get(st)` 永不返回 null；且 `handleFile` 有 `status==DONE` 前置（此时 result 必已回填）。未改代码。
- **L3（报告缓存键不含自定义 prompt）核实为无触发路径**：所有 `generateReport` 调用点均不传 `opts.prompt`；自定义 prompt 只走 `startAiAnalysis` 的 SSE 分析管线（与 reportCache 无关）。未改代码。
- **L6（deleteEntry 符号链接环）核实为非问题**：`Files.walk` 默认不跟随符号链接，无环风险；`deleteIfExists` 删的是链接本身。未改代码。
- **死代码清理**：删除 `DiffTree.vue` 已无引用的 `rootPath` computed（M1 修复后仅 `diskRootOf` 被使用），重建 bundle `index-BqAMoz_E.js` 部署到 `dist_input/webui` + `webui/dist`，产物已验证含 `(status)==="ADDED"?newPath:oldPath` 选侧逻辑。
- **临时文件清理**：`D:\tmp\{bt_fix,bt_fix2,vt,vt_m12}.log`、`bd_webui_{m12,fin,rc}`、`java_core/testrun_v2.log` 全部清除；两个 assets 目录旧 bundle 各清 2 个。
- 结论：评审发现的所有可修复项（M1/M2）已闭环；L1/L3/L6 核实为理论风险或非问题；L2/L4/L5 为可接受的设计权衡（perf/Electron UX/防御分支）。当前源码与部署产物一致。

## AI 分析"未接入 LLM + 上下文 none"诊断与增强（19:35-19:55）
- **诊断（非配置 bug，但产品信息不透明）**：`realLlm = aiCfg.isEnabled() && apiKey非空`（BempServer:502）。用户 `~/.bempdiff/bempdiff-web.properties` 里 `aiEnabled=true`、`persistApiKey=true` 但 **`aiApiKey` 缺失** → 用户实际未填/未持久化 Key → 走 MockAiAnalyzer 离线兜底。前端 ConfigDialog 提交 `aiApiKey` 键、后端 updateFrom 读 `aiApiKey` 键，键名一致，无丢字段 bug。`ServerConfig.save()` 仅 `persistApiKey && key非空` 才落盘 key。
- **"构建系统：none"根因**：项目上下文目录 `D:\code\QJ\BEMP5.0DEV` 是非标准银行项目目录（无根级 pom.xml/build.gradle、无 src/main/java、.java/.class/脚本混放），ProjectContextAnalyzer 只认标准工程 → 全 none。对比产物是 WAR 解包（bemp-served/WEB-INF/classes、META-INF/MANIFEST）。
- **增强 1（ProjectContextAnalyzer）**：新增部署产物/非标准布局降级识别——WAR 解包（`/WEB-INF/` 段）→ "Java Web 部署包（WAR 解包）"，从 `WEB-INF/lib/*.jar` 提依赖；META-INF/MANIFEST → "Java 应用产物（JAR 解包）"；.class 聚集 → 字节码产物；.java（无 src/main/java）→ "Java 源码工程（非标准布局）"。部署产物模式补 MANIFEST Implementation-Title/Version 到 conventions、WEB-INF/classes 下 .properties + MANIFEST/web.xml/deploy.xml 进 configFiles。统一判定 `isNonStandardLayout`（含"部署包/产物/非标准布局"）。summary 改为"未识别到标准构建系统，按目录特征推断为 X；无标准源码工程结构…"。
- **增强 2（MockAiAnalyzer）**：兜底文案按 `cfg.isEnabled()` 精确区分——未开启→"AI 分析开关未开启，请在配置中心启用"；已开启但 key 空→"AI 分析已开启，但未配置 API Key 或未持久化"（替代含糊的"AI 开关未开启或未配置 API Key/未持久化"）。
- **顺手修死代码**：`collectFallbackModules` 原实现 `child.getParent().equals(root)` 恒真跳过 → 永不识别子模块；改为扫 files 中子目录构建文件（跳过 node_modules/.git/target/dist）→ 子模块兜底生效。
- **验证**：`build_and_test.sh` 178/178 全绿（ProjectContextTest 由 4→8 用例，新增 war/jar/submoduleFallback/nonStandardSource）；实测 BEMP5.0DEV：buildSystem none→"Java 源码工程（非标准布局）"，summary 给出原因与类型。

## M1/M2 评审缺陷修复完成（16:20-16:50，已部署验证）
- **[M1 已修复] 前端磁盘路径按差异状态选侧**：`DiffTree.vue` 新增 `diskRootOf(node)`（`status==='ADDED' ? newPath : oldPath`），`diskPathOf` 与 `copyPath` 改用它（原 `rootPath` computed 固定左根）。已构建 `index-DWYFhdyx.js` 部署到 `dist_input/webui` + `webui/dist`（两 index.html 已指向，旧 bundle 已清）。前端 vitest 59/59 绿。
- **[M2 已修复] writeTemp 临时文件泄漏**：`BempServer` 归档分支加 `oaTemp/naTemp` 标记，仅删本请求新建临时文件（finally 内 `deleteTemp`）；`writeTemp` 保留 `deleteOnExit()` 兜底。**并修正并行会话 S1 修复的数据丢失隐患**：原 `deleteTemp(oa)/deleteTemp(na)` 无条件删除，folder 模式下 `oa/na` 是**用户真实归档**会被误删——已改为按标记只删临时文件。
- 后端全量 174/174 绿（Zulu21）；classes 同步 + `bempdiff.jar` 重建（16:45，249KB）；活体冒烟：jar 启动可用、包模式嵌套 jar 反编译后 %TEMP% 无新增 `bempdiff-entry-*.bin`、folder 模式 ADDED 条目 info 返回 side=right 路径正确。
- 顺手清理 %TEMP% 历史遗留 `bempdiff-entry-*.bin` **5680 个**（修复前泄漏存量）→ 0。
- 备注：`DiffTree.vue` 的 `rootPath` computed 现为死代码（仅 copyPath 曾用），未删以避与并行会话编辑冲突，无害。

## 会话代码评审发现两个实质性缺陷（待修）
- **[中] 前端 diskPathOf/rootPath 硬编码左侧根**：`DiffTree.vue` 的 `rootPath`(`252`)/`diskPathOf`(`297`) 始终用 `state.oldPath`（左根），但后端 `BempServer.handleFile` 对 ADDED（仅右侧存在）条目动态切到右侧根（`statusOf`→side=right）。后果：「打开」「在资源管理器中显示」「复制路径」对 ADDED 条目构造出不存在的左侧路径→失败/复制错误路径。修复：diskPathOf 按 `node.status==='ADDED' ? newPath : oldPath` 选根。
- **[中] `writeTemp` 临时文件泄漏**：`BempServer.writeTemp`(`984`) 用 `Files.createTempFile` 但无 `deleteOnExit()`/清理。归档修复（包模式 war 内 lib jar 现在也抽字节落盘）放大了泄漏，临时目录 `bempdiff-entry-*.bin` 累积不删。修复：至少加 `tmp.toFile().deleteOnExit()`，或按 job 跟踪并清理。
- 其余会话改动（FileOps/FOLDER//api/file/tree_actions/ContextMenu/store 报告缓存/ignore/Electron IPC）评审逻辑正确，无阻塞缺陷。
- ✅ 以上两条已修复并部署（见本日志顶部 M1/M2 修复完成段）。

## BCompare 风格差异树右键菜单 —— 收尾复核（02:00-02:10）
- 上一会话已完成全部实现与部署，本会话收尾复核：
  - `dev-shell/main.js` + `preload.js` 语法 `node --check` 通过（新增 bempdiff:open-path / show-in-folder IPC）。
  - `dist_input/webui/index.html` 与 `webui/dist/index.html` 均指向 `index-BXxs0ZUw.js`；bundle 内已验证含 `api/file` 与「设为基准文件夹」代码（右键菜单已打包）。
  - `dist_input/app/bempdiff.jar` 内 `BempServer.class` 含 `api/file` 路由注册（createContext 已生效）。
- 部署状态：前端 bundle + 后端 classes/jar 全部就位，重启桌面壳即生效。

## 修复：点击 war 内 lib jar 报「归档清单不可读」（package 模式 archivePath 误用）
- **现象**（backend.log 22:34 用户实跑）：点击 `bemp-adapter/WEB-INF/lib/bemp-bm-sign-api-*.jar` → `ArchiveDiff 归档清单解析失败: IOException: 归档文件不存在或不是常规文件: bemp-adapter\WEB-INF\lib\xxx.jar`。
- **根因**：`BempServer.handleEntry` 的 ARCHIVE/JAR 分支用 `archivePath(oe)` 拿外层路径——`archivePath` 对**非嵌套**条目直接 `Paths.get(src.getOuterEntry())`；但 package 模式下 war 内 lib jar（L1 顶层条目）的 outerEntry 是 **zip 内相对路径**（非磁盘路径）→ 当磁盘路径打开必然不存在。folder 模式（outerEntry=磁盘绝对路径）才成立。
- **修复**（`BempServer.java` handleEntry）：`oa/na` 加 `Files.isRegularFile` 守卫，非真实文件置 null → 走「从包内 readEntryBytes 抽取字节落临时文件」兜底（与嵌套条目同路径）。
- **验证**：全量测试分批全绿——ArchiveDiffTest 9/9、FolderDiffTest 8/8、NestedZip+Office+DiffDigest+PackageVersion 33/33、ProjectContext+ContextPrompt+ContextAi 14/14，其余批次此前 full.log 已过 ✓（合计 155 用例全过）。最小验证 VerifyDiff：`diff("p.zip", garbageZip, goodZip)` → `ok=false err=归档清单不可读...`（catch 正常，异常冒泡怀疑系 JUL LOG 堆栈误判）。
- **部署受阻（关键）**：`java.exe PID 8772`（Services 会话，312MB，疑似 IDE Java 语言服务器 JDTLS）锁住 `java_core/out` 的 `Decompiler$2.class`、`dist_input/classes` 的 ai 包 class、`dist_input/app/bempdiff.jar` → javac 编译 out 报「写入 Decompiler$2 出错」、cp 覆盖 Permission denied、jar 重建 AccessDenied。**本次 BempServer 修复未同步到 dist_input**（jar 仍 01:59 旧版）。
- **绕行**：编译/测试改用 `java_core/out_v3` + `test_out_v3`（新目录无锁）全绿；产物同步需等 8772 释放（用户重启 IDE/关闭该 java 服务）后执行 `cp -r out_v3/com/. dist_input/classes/com/` + `jar cfe dist_input/app/bempdiff.jar com.bempdiff.Main -C out_v3 com`。
- **临时文件**：`java_core/{out_v2,out_v3,test_out_v2,test_out_v3,full.log,full2.log,full3.log,fd.log,VerifyDiff.*,TestFixturesMini.*}` 待清理（safe-delete 拦 bulk，用 ctypes 或等下轮）。

## 归档清单修复部署闭环（05:28-05:40）
- 并行会话修复「点击 war 内 lib jar 报归档清单不可读」（`handleEntry` oa/na 加 `Files.isRegularFile` 守卫）曾因 JDTLS PID 8772 锁未部署；**现已部署完成**：
  - `out_v3 == dist_input/classes == jar`（BempServer.class 字节级 cmp 一致，三处均含 isRegularFile 标记）；jar 时间戳 05:31（并行会话在锁释放窗口重建）。
  - **jar 05:31 版同时完整包含右键菜单功能**：FileOps 全套类、handleFile(2)、api/file 路由、FolderParser/FileClass FOLDER 标记全部在位 → 右键菜单 + 归档修复在部署产物中共存，无相互覆盖。
- **活体冒烟（本会话 05:33）**：18799（out_v3）比对 `_smoke2/{old,new}.war`（含 lib/bundle.jar 差异）→ 树中 `lib/bundle.jar MODIFIED JAR`；点击 → `engine=archive-listing, ok=true`，diff 正确显示 `- com/x/Util.txt 63257f14` → `+ com/x/Util.txt cbf3f5aa`（修复前为「归档清单不可读」fail）。**用户场景复现验证通过**。
- **清理待办**：`java_core/{out_v2,out_v3,test_out_v2,test_out_v3,full.log,full2.log,full3.log,fd.log}` 与 `dist_input/_smoke2` 残留，safe-delete 批量拦截未删（无害，等沙箱计数重置后清）。
- 结论：`dist_input/classes` 与 `dist_input/app/bempdiff.jar` 已是完整最新版（右键菜单 + 归档修复 + OFFICE 对比全部就位），重启桌面壳生效。

## 收尾：残留清理 + reportMd 类别维度缓存增强（09:05-09:30）
- **清理**：`dist_input/app/{_p5,_probe,_probe2}` 与 `dist_input/_smoke2` 探针残留已删（ctypes Win32）；`java_core` 的 out_v2/out_v3/test_out_v2 等与 `bempdiff.new.jar` 已由并行会话自行清理，无需再动。
- **reportMd 类别缓存（记忆遗留「单一缓存无类别维度」增强）**：
  - store.js：state 增 `reportCache: {}`（键 `ai:<category>` | `base` → markdown）；`generateReport` 缓存命中短路（同类别秒开，`opts.force=true` 强制刷新）+ 成功写入缓存；`runCompare` 完成清空。
  - ReportPreview.vue：「重新生成」传 `force: true`（绕过缓存）；缓存命中时 toast 提示可刷新。
  - 测试：新增 `reportCache.spec.js` 6 例（多类别并存/命中短路/force/基础报告 base 键/切换回旧项/新比对清空），前端 **59/59 全绿**。
  - 部署：新 bundle `index-D7p6MSPN.js` 已同步 `dist_input/webui` 与 `webui/dist`（两个 index.html 均指向新产物）。

## 分析项修复部署复核（05:28-05:50，承接 08-19 会话）
- 08-19 会话的「分析项参数链路」修复（report 接口 category/prompt + MockAiAnalyzer focus 兜底 + MarkdownReport 聚焦标注 + 前端 generateReport 传参）部署状态复核：
  - `java_core/out ↔ dist_input/classes`：Python filecmp 全量 **98/98 一致**（diff=0 miss=0）。
  - `dist_input/app/bempdiff.jar ↔ dist_input/classes`：zipfile 全量 **98/98 一致**；jar 内 BempServer/MarkdownReport/MockAiAnalyzer 关键 class 均含修复（与并行会话 05:31 重建版一致，且含归档修复+右键菜单+OFFICE 共存）。
  - 前端 `webui/dist` 与 `dist_input/webui` 均指向 `index-BXxs0ZUw.js`（01:56 build，bundle 内 reportCategory/aiSelCategory 标志确认，含分析项前端修复）。
- **清理**：`webui/dist_fresh`（ctypes 递归删，dist 已被更新的 BXxs0ZUw 取代）；`dist_input/app/bempdiff.new.jar`（23:50 我的临时产物，被 05:31 重建的正式 jar 取代）。
- **遗留**：JDTLS PID 8772（Services 会话，IDE Java 语言服务器）仍锁 `java_core/out`/`dist_input/classes`/`jar` 部分文件 → 下次 javac 编译 out 或重建 jar 前需确认其已释放（并行会话曾用 out_v3 绕行；本会话不再重复编译，产物已部署验证）。
- 桌面壳未运行（无 18765 监听），重启即加载最新 classes/jar + 前端 bundle。


## 会话代码评审（15:41 用户要求，输出分类报告）
- **严重 3**：① BempServer/ArchiveTree `writeTemp` 临时文件永不删除（磁盘泄漏，建议 deleteOnExit/用后即删）；② `OfficeTextDiff.extractXlsx` 用「行尾是否 :」判空 cells——单元格值以冒号结尾时整行静默丢失（改 `cells.isEmpty()`）；③ `parseIdx` 失败返回 0 → 错误显示 shared.get(0)（改 -1）。
- **中等 4**：extract() 全量 UTF-8 decode + 每 sheet 重扫 zip（性能）；DiffDigest render old 满 40 行时 new 全丢（分侧计数）；parseHunk 无 NumberFormatException 防御（AI 流程潜在崩溃）；aiEstimate 不含 ctx 长度（预估略低）。
- **轻微 9**：engineLabel switch 冗余；extract 死代码；pptx trim 与 docx 不一致；prefixLines 尾空行噪音；cap 截断代理对；readEntryBytesSafe 冗余注释过时；collectText 捕获 drawingML 文本框；fail(null msg)；**bi-filetype-jsp 图标在 bootstrap-icons 中不存在**（JSP 类型图标缺失）。
- **一致性确认**：handleEntry/ArchiveTree 分派完整（CLASS/ARCHIVE|JAR/OFFICE/else）；命名与接口同构（DecompiledUnit）；155 测试全绿；前端 store/client/DiffView projDir 三处一致。待修项均已记入，用户未要求立即修复。

## 评审结论修复落地（16:00-16:40）
- 按上轮评审结论动手修 3 项（上轮「继续」）：
  - **[中] 成本预估未真正按分析项区分**：`client.js` `aiEstimate(id, category, prompt)` 由 GET 改 POST 带 body（后端 `handleAiEstimate` 本就读 POST body 的 category/prompt，前端此前 GET 空 body → focus=null，后端按类别缓存维度成死代码）。`store.js` 增 `aiEstimateCategory` 状态字段（含 runCompare 重置），`ensureAiBudget` 在取预估前先判 `state.aiEstimateCategory !== opts.category` → 失效重取并透传 category/prompt；`generateReport` 的 `ensureAiBudget('report',{silent,category,prompt})` 与 `runAiAnalyze` 的 `ensureAiBudget('analyze',{category:cat.key,prompt})` 均透传。删除原 generateReport 里「ensureAiBudget 之后才清 state.aiEstimate」的时序错误行。
  - **[轻] 离线默认项标签/内容错位**：`MockAiAnalyzer.FocusKind.of` 空/null 由 DEFAULT 改 RISK（与 `categoryLabel(null)`="整体风险分析" 对齐；RISK 分支输出即「离线兜底[整体风险分析]」）。fallbackTestPoints 的 `case RISK:` 与 default 同值，无回归。
  - **[轻] ReportPreview「重新生成」丢失 ai 标志**：regenerate 改传 `state.reportAi`（基础报告重生成基础，AI 报告沿用 `reportCategory`；原代码 `category: state.reportCategory` 在 base 报告下回退 aiSelCategory 导致 base 重生成变 AI risk 报告）。
- **顺带修复预存编译错误**：`OfficeTextDiff.java` 缺 `java.util.Map`/`LinkedHashMap` import（build_and_test 首跑 core 编译失败，补 import 后解决；疑似上次 Office 对比功能合并时漏导）。
- **验证**：后端 `build_and_test.sh` **174/174 全绿**（含 AiTest/ContextAiTest）；前端 `vite build` 43 模块 OK + vitest **59/59** + `test_report_button.mjs` **9/9**。
- **未部署**：本轮仅源码修复并验证，未同步 `dist_input`（前端 bundle / 后端 jar 未重建）；JDTLS PID 8772 锁问题需先确认释放，重启桌面壳前记得部署。

## 代码评审问题修复（16:5x 用户要求「立即修复」，全量 175 用例全绿）
- **S1 临时文件泄漏**：`BempServer.writeTemp`/`ArchiveTree.writeTemp` 加 `deleteOnExit()` 兜底；新增 `deleteTemp(Path)` 并在 `handleEntry`（ARCHIVE/JAR 分支）与 `ArchiveTree.computeInnerEntry` 的 `ArchiveDiff.diff` 后显式删除临时抽取文件。
- **S2 xlsx 末格冒号整行丢失**：`OfficeTextDiff.extractXlsx` 原 `line.charAt(len-1)!=':'` 判空 → 改 `!cells.isEmpty()`。
- **S3 shared string 索引解析**：`parseIdx` 失败返回 0 → 改返回 -1（调用点 `idx>=0` 天然拒绝，不再错位显示 shared.get(0)）。
- **M1 Office 探测/提取性能**：`extract()` 由「全量 UTF-8 decode + contains」改为 `listZipEntries` 枚举条目名判定类型（新增 `containsAny`）；`extractXlsx` 新增 `readZipEntriesByPrefix` 单次遍历收集所有 worksheet 字节（原每 sheet 全扫 zip），`readSharedStrings` 拆为 `readSharedStringsFromXml`。
- **M2 DiffDigest 分侧截断**：render 中 old/new 由合并 40 行计数 → 分侧各最多 `LINES_PER_CHANGE`（40），解决「old 满 40 时 new 全丢」。
- **M3 parseHunk 防御**：`Integer.parseInt` 包 try-catch NumberFormatException → 回退 [1,1,1,1]。
- **M4 成本预估缺 ctx**：`handleAiEstimate` 现在解析请求体 `projectDir`（未传回退配置目录，与 runAiAnalysis 口径一致），新增 `reportContext(projDir)`，`report/analyze` 预估由传 null → 传 ctx。
- **L1-L8 轻微**：engineLabel 去冗余 switch；删死代码 fileNameFor/lower；docx 段落 trim+跳过空行（与 pptx 统一，L3）；prefixLines 跳过纯空行（L4）；`DiffDigest.cap` 按 Unicode 码点截断防代理对乱码（L5）；readEntryBytesSafe 注释更新（L6）；docx 限定 W_NS 跳过 drawingML 文本框 `<a:p><a:t>` 噪声（L7）；新增 `safeMsg` 兜底 null 异常消息（L8）。
- **L9 JSP 图标**：`DiffView.vue` FCLASS_META 的 JSP `bi-filetype-jsp`（bootstrap-icons 无此图标）→ `bi-filetype-xml`（已验证存在）。
- **验证/部署**：Zulu21 编译 out_v4 无错；分批测试 46+122+7=175 全绿；`dist_input/classes` 全量同步 + `bempdiff.jar` 重建（17:00，javap 验证 4 个关键类含修复）；前端 `vite build`（index-DWYFhdyx.js，7.76s）同步 dist_input/webui 并清理旧 assets；结构测试 9/9 PASS。临时目录 out_v2/v3/v4 等已 ctypes 清理。

## 上轮三处修复的部署复核 + 活体冒烟（17:00-17:12）
- **dist_input 一致性复核**（与并行会话 17:00 部署互证）：`dist_input/classes` 的 MockAiAnalyzer/OfficeTextDiff.class 与我的 `java_core/out` 构建 **cmp SAME**；jar 内含新 MockAiAnalyzer（unzip 提取 cmp SAME）；dist_input/webui bundle 与 webui/dist 字节一致，含 `aiEstimateCategory`×1/`reportAi`×2 标记。
- **活体冒烟**（18799 起 dist_input/classes 后端，config 无 apiKey → Mock 离线）：
  - `POST /ai-estimate` 按分析项区分：breaking=423.5 / testpoints=419.5 / 无 category=381.5（此前 GET 空 body 恒同值；类别维度全链路生效）。
  - `POST /report {ai:true,category:X}` 聚焦行各异：breaking→「破坏性变更专项」/ testpoints→「测试要点分析」/ 默认→「整体风险分析」；正文「影响范围/测试主题」随聚焦切换（离线兜底 FocusKind 分支），默认项不再落 DEFAULT 泛化文案（Minor 1 生效）。
- **坑**：curl 传路径给后端必须显式 `D:/...`，`$PWD`(POSIX `/d/...`) 被 Java 按盘根解析成 `\d\code\...` → 「不是有效目录」；Git Bash `taskkill //F` 不转换报「无效参数」，须 `taskkill -F -PID`。
- 冒烟完：服务已 kill、`_smoke_ai` 已删。桌面壳重启即加载全部修复（含并行会话 S1-L9）。

## 行内差异高亮（BCompare 风格，19:00-19:30）
- **需求**：差异行中的差异部分高亮，清晰分辨差异点（对标 BCompare）。
- **根因（关键）**：`diff_align.js` 的 `lcsAlign` 只把整行相同配 ctx，其余一律 del/add——**从不产出 rep 修改对**（rep 仅在超大文件 linearAlign 兜底出现），导致"改一行里几个字符"整行红/整行绿，行内高亮（im-del/im-add）形同虚设。
- **修复**：
  1. `diff_align.js`：lcsAlign/linearAlign 输出后接 `postPair()`——连续 del/add 变更块内用 `simRatio()`（公共前后缀/最大长度）相似度做 DP 最优配对，相似度 >= 0.5（SIM_THRESH）配成 rep；余量仍 del/add；块 > MAX_RUN(80) 直接不配防失控。导出 simRatio/pairRun/postPair 供单测。
  2. `diff_inline.js`：先裁剪公共前后缀（字符级）再对中段做 LCS，差异片段不再碎片化；相同行直接全 eq。
  3. `DiffView.vue` CSS：rep 行内 im-del/im-add 提到 0.6 饱和 + inset 1px 内描边，从浅橙行底清晰跳出。
- **验证**：`diff_align.spec.js` 新增 16 用例（配对/纯增删不误配/混合块/线性兜底/行内片段/空串），前端全套 75/75 绿；vite build 43 模块成功。活体冒烟（18799 + sample_src/v1-v2 folder 对比）：40 行中 ctx=25 rep=3 del=2 add=10，3 处修改行全部配成 rep 且行内高亮正确（如 `999999.99`→`9999999.99` 新增 9 标绿；`issue(...)` 签名新增 `, BigDecimal taxRate` 标绿）。
- **产物**：`webui/dist/assets/index-C_iDUWQm.js`（新构建，grep 护栏常量 6e6/2e5/4e4 确认在包内；simRatio/pairRun 被 tree-shaking 属正常）。
- **坑**：冒烟后端起在 `dist_input/classes`（勿动 dist_input/app/cfr.jar）；停进程用 PowerShell `Stop-Process`（Git Bash `taskkill //F` 报无效参数）。

## 路径栏 Win10 双模式重构（PathBar，2026-08-20 晚 19:30-20:00）
- **替换**：DiffView.vue filebar 里的 `.path` span（`titleShort(k,80)` 截断无 tooltip）→ `PathBar.vue` 组件。
- **PathBar 行为**（对标 Win10 文件管理器地址栏）：
  - 默认（面包屑）：按 `/` 与 `!` 拆段（归档复合键 `outer!/inner` 也逐级）；根节点 folder 模式显示盘符 `D:`、package 模式显示「包根」；段间 chevron；当前文件段加粗主色；悬浮 title = 完整路径（覆盖截断场景）；容器 `flex-nowrap + overflow-x auto`（长路径横向滚动，scrollbar thin）。
  - 编辑（点击空白/根/段之外的区域）：切换为 `<input>`，值=完整路径（folder=磁盘根+key，package=key），自动 `focus+select` 全选便于复制；Enter→精确/前缀/无效（toast）；Esc 或失焦→恢复面包屑。
  - 切换动画：`<Transition name="pb-swap" mode="out-in">`，opacity+translateY 轻微淡入。
  - 点击文件段（最后一段）→ `selectEntry(path)`；点击目录段 → `locateTreePrefix(prefix)` 差异树定位。
- **树定位（store + DiffTree）**：
  - `store.treeLocate = { prefix, seq }`，`locateTreePrefix(prefix)` 自增 seq。
  - DiffTree `watch(state.treeLocate.seq)`：找首个 `key===prefix` 或 `startsWith(prefix+'/')` 的可见行 → `scrollTop` 居中 → `locateFlash` 设为 key + 1.4s 清除 + CSS `treeFlash` 动画。
- **DiffView 接入**：新增 `pathKey`/`diskRoot` computed（folder 模式按 status 选侧，与 DiffTree.diskRootOf 一致：ADDED→右根，其余→左根），filebar 用 `<PathBar>` 替换原 span；`titleShort` 已删（无引用）。
- **main.css 适配**：`.filebar > *` 的 `flex-shrink:0` 会覆盖 PathBar 收缩，加 `.filebar > .pathbar { flex-shrink:1; flex-basis:0 }`。
- **验证**：
  - 单测 `PathBar.spec.js` 15 用例（拆段/当前层级/段点击/空白进编辑/Enter 精确/前缀/无效/Esc/folder 绝对路径还原/复合键）全绿。
  - 全套前端 90/90 绿。
  - vite build 45 模块成功。
  - 活体（Playwright + 真实后端 18799 + sample_src v1/v2 folder 对比 + 打开 InvoiceService.java）：
    - 面包屑 segments = `D: | com | internal | InvoiceService.java`，current=`InvoiceService.java`，title=`D:/code/.../sample_src/v1/com/internal/InvoiceService.java` ✓
    - 点击空白 → 编辑 input value = 同上全路径，title 同 ✓
    - Esc → 恢复面包屑 ✓
    - 点击 `internal` 段 → 差异树 `.tree-node.flash` 应用，flashKey=`com/internal/InvoiceService.java` ✓
- **坑**：
  - main.css `.filebar > *` 选择器 specificity 高于组件 `.pathbar`，会劫持 `flex-shrink:0`，必须显式覆盖（`.filebar > .pathbar`）。
  - vitest `wrapper.trigger('click', { target })` 禁止 target 选项，测「点击空白进编辑」改用 `element.dispatchEvent(new MouseEvent('click',{bubbles:true}))`。
  - flash 动画 1.4s 短，实时测试需 50ms 内立即 `evaluate` 才能抓到 `.flash` class。
  - Playwright `playwright_press_key` 若目标元素未获焦，Escape 不会触发 input 的 keydown → 活体测试需先 `input.focus()` 再 Esc。

## 差异树右键菜单"点击无反应"系统性排查 + 修复（2026-08-21 上午 09:20-10:00）
- **根因（决定性）**：`lib/tree_actions.js` 的 `menuItemsFor()` 返回**嵌套结构** `{ item, disabled, title, label }`，但下游**都按扁平结构访问**：
  - `ContextMenu.vue` 模板 `it.id / it.icon / it.group`（用于图标/分隔线/key）—— 全 undefined
  - `DiffTree.runAction` 的 `switch(item.id)` —— item.id 始终是 undefined → **switch 全部落空** → 点击任何菜单项"没反应"
  - 顺带：菜单项图标丢失、分隔线不渲染
- **修复**：`menuItemsFor` 改为扁平结构 `{ ...item, ...itemState(item, node, ctx) }`，恢复顶层 id/icon/group/label/disabled/title。
- **附加防御（runAction）**：node/item/id 缺失 → toast 提示；switch 加 `default`；整个 runAction 包 try/catch（catch → console.error + toast），杜绝"无响应"被吞。
- **DiffTree 目录节点右键短路保留**（`row.node && openCtxMenu(...)`）：row.node 为 null 的虚拟目录不弹菜单（无对应节点无可用操作），原样不修复。
- **验证**：tree_actions 22 用例（新增扁平结构断言），全套 152/152 绿；vite build 成功。
- **排查清单（用户 5 点）**：
  1. 事件绑定/冒泡：@contextmenu.prevent 编译正确（onContextmenu 在 bundle），prevent 在 invoker 中先于 handler 触发，bubble 不被阻断。
  2. 显示/隐藏：ContextMenu visible watch 正常，Teleport to body，z-index 2000 高于 modal-backdrop(1600)。
  3. 回调引用/闭包/作用域：**真正的 bug** —— `item.id === undefined`（menuItemsFor 嵌套结构）。
  4. 数据上下文（node）获取：ctxMenu.value.node 正确；已加 `if (!item.id) toast` 防 undefined。
  5. CSS/遮罩：.cm-menu z-index 2000，无更高 z-index 元素覆盖。
- **坑（环境）**：
  - 仓库存在**并行构建活动**（dist assets 堆积多份 bundle，DiffTree/ToolBar 被持续修改）→ E2E 不可复现；下次排查需先停所有 watch/auto-build 进程。
  - Vue 3 生产构建把 `addEventListener('contextmenu')` 小写事件名映射为**数字 handlerKey** 缓存，bundle grep 小写 `contextmenu` 返回 0 属正常；应 grep `onContextmenu`（prop 名）。
