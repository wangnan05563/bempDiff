# 差异分析报告（Java 端口 / 真实包比对）

- 老包：`jsstage_v1.zip`（版本 1）
- 新包：`jsstage_v2.zip`（版本 2）

## 一、差异统计
- 新增 **0** · 删除 **0** · 修改 **1** · 未变 1
- 业务/类级变更(非jar)：1 · jar 级变更：0

### 按文件类型分布
| 文件类型 | 新增 | 删除 | 修改 |
|---|---|---|---|
| 前端 JS | 0 | 0 | 1 |
| **合计** | **0** | **0** | **1** |

## 二、差异文件树（全量清单）
- `修改 ~` index.js

## 三、反编译源码级差异（Top-5 修改/新增/删除 Java 类）
> 共 0 个类已反编译（本处仅展示前 Top-K 条，且每条仅列变更行：附原始/修改后行号与变更类型——修改/新增/删除，不展示整个文件）。

## 四、文本类文件内容差异（Top-5 修改/新增/删除 配置文件/JSP/JS/HTML/CSS）
> 共 1 个文本类文件已做内容级逐行 diff（本处仅展示前 Top-K 条，且每条仅列变更行：附原始/修改后行号与变更类型——修改/新增/删除，不展示整个文件）。

### index.js
- 处理引擎：js-beautify
```diff
- [新增] 新 L2：
+    const FREE_THRESHOLD = 200;

- [新增] 新 L4：
+      if (price >= FREE_THRESHOLD) return 0;

- [修改] 老 L5-6 → 新 L7-8：
-    export function format(price) {
-      return 'CNY' + price.toFixed(2);
+    export function calcSurcharge(price) {
+      return price > 0 ? price * 0.02 : 0;

- [新增] 新 L10-12：
+    export function format(price, hasTax) {
+      return 'CNY' + price.toFixed(2) + (hasTax ? ' inc tax' : '');
+    }
```

## 五、差异依赖 JAR 内部源码对比（Top-5 内部 class 反编译源码级 diff）
- 无差异依赖 JAR（本次比对未检出 JAR 级依赖变动）。

## 六、破坏性变更清单（删除类/删除前端资源）
- 无删除类。

## 七、AI 智能分析（两阶段 / 项目级上下文增强）
### 阶段A 概览
- 整体风险：**MEDIUM**
- 影响范围：价格计算模块及格式化接口，涉及税费计算逻辑和价格展示格式
- 测试主题：财务计算准确性测试（含免税门槛和附加费逻辑）；价格格式化输出验证（含税标识显示）；边界条件测试（价格等于FREE_THRESHOLD时的处理）；接口兼容性测试（原有format接口已废弃）
- 每文件初评风险：
  - `index.js`：**MEDIUM** — 修改了核心价格计算逻辑并重构格式化函数，可能影响财务准确性与前端展示

### 阶段B 逐文件深读
#### index.js
- 风险：**HIGH**
- 意图：重构价格计算逻辑并扩展格式化功能
- 影响：- page：可能影响价格展示组件的显示逻辑
- interaction：可能导致价格计算交互结果异常
- api：可能改变价格计算接口的返回值结构
- 测试要点：
  - 验证calcSurcharge函数在price=200时返回0（原L5-6位置）
  - 测试format函数新增hasTax参数对显示的影响（新L10-12）
  - 检查旧format函数调用是否已全部替换为新函数（原L5-6位置）
  - 验证价格计算逻辑在不同阈值下的正确性（新L7-8）
  - 测试价格展示组件在有税/无税场景下的显示差异


## 八、审计摘要（基础）
- 比对时间：2026-08-31 20:18:36
- 老包标识：1
- 新包标识：2
- AI 分析：已接入（含项目级上下文增强）
