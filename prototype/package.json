{
  "name": "bempdiff",
  "version": "0.1.0",
  "private": true,
  "description": "BempDiff 桌面应用根（Tauri 2.x + Web 前端 + Java 后端 sidecar）",
  "scripts": {
    "dev": "cd webui && npm run dev",
    "build": "cd webui && npm run build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
