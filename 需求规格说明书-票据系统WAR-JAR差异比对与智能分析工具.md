# 需求规格说明书：票据系统 WAR / JAR 差异比对与智能分析工具

| 项 | 内容 |
| --- | --- |
| 文档名称 | 票据系统 WAR/JAR 差异比对与智能分析工具 需求规格说明书 |
| 文档版本 | v1.1 |
| 状态 | 评审修订稿（v1.1） |
| 编写日期 | 2026-08-07 |
| 修订日期 | 2026-08-07 |
| 适用范围 | 票据系统（BEMP 5.0）发版/升级场景下的版本差异比对与评估 |
| 关联系统 | BEMP 5.0 研发环境 `D:\code\QJ\BEMP5.0DEV`（Maven 多模块：55 个 pom 模块、58 个 jar 模块、27 个 war 模块） |

> 说明：本工具的目标用户是 **实施/运维/测试/研发** 人员。在每次产品部下发新版本 war/jar 包后，用户用本工具对比"生产环境当前版本包"与"产品部新下发版本包"的差异，借助反编译与大模型能力，快速得出"改了什么、风险多大、影响哪里、要测什么"，并产出可流转的分析报告与差异资产。

### 修订记录（v1.0 → v1.1）

本节针对 v1.0 评审意见逐项修订，共 11 项：

| # | 评审意见 | v1.1 修订 |
| --- | --- | --- |
| 1（阻塞） | 差异粒度策略混乱（jar级 vs class级、内部 vs 第三方未厘清） | 新增 §5.2.1「差异粒度分层策略」L0/L1/L2，路径命名空间避免冲突 |
| 2（阻塞） | AI 分析规模与成本未定义 | 新增 §5.5.1「两阶段分析策略」概览→深读，定义单位/截断/限额 |
| 3（高） | Fat jar 与 war 解析路径不一致 | §5.2 新增「按包类型的路径映射表」（war / 普通 jar / fat jar） |
| 4（高） | 金融合规未作为一等需求 | 新增 §5.9.1「金融合规与审计」；非功能需求加合规/审计行；新增 AC9 |
| 5（高） | 删除类分析短板 | FR4.2/4.3 覆盖删除类；新增 FR4.7 删除类专项汇总 |
| 6（高） | 非文本比对边界不清 | 新增 FR4.8 非文本仅状态/大小，不做内容 diff |
| 7（中） | 版本标识应自动提取 | 新增 FR1.7 自动提取 MANIFEST/pom.properties 版本 |
| 8（中） | 代理配置缺失 | 新增 FR9.11 代理配置 |
| 9（中） | UI 布局含糊 | 新增 §5.10「UI 布局与线框」 |
| 10（中） | 配置覆盖关系不清 | §5.2.1 明确 FR2.2 与 FR3.6 的关系与叠加规则 |
| 11（中） | 报告默认体量未定义 | FR7.2 明确默认含「全量清单 + Top-K 完整 diff + 总结」 |

---

## 1. 背景与目标

### 1.1 业务背景

- 票据系统（BEMP 5.0）是 **Maven 多模块 Java 工程**，构建产物主要为 `war` 包（27 个 war 模块）与 `jar` 包（58 个 jar 模块），由产品部定期编译打包后下发。
- 用户侧（实施/运维）持有 **生产环境当前在用的版本包**；产品部下发新版本包后，需评估"新老版本到底差在哪"。
- `war` 包本质是 ZIP：含 `META-INF/`、`WEB-INF/classes/`（编译后的 `.class`）、`WEB-INF/lib/*.jar`（第三方与内部 jar）以及静态资源（jsp/html/js/css/图片等）。其核心逻辑都在 `.class` 字节码里，肉眼不可读。
- **关键现实**：BEMP 的 27 个 war 会把 58 个**内部 jar** 打进 `WEB-INF/lib`。真正的"业务改动"主要在内部 jar 的 class 里，而非 `WEB-INF/classes`。因此差异分析必须以"内部业务码"为主视角（见 §5.2.1）。

### 1.2 现状痛点

1. **比对困难**：war/jar 是二进制压缩包，无法直接用文本 diff 对比；`.class` 文件不可读，必须反编译。
2. **无差异清单**：缺少"全量差异文件树"，升级时容易漏看被改动的类或配置。
3. **评估靠人肉**：改动意图、风险、影响范围、测试要点全靠有经验的开发逐包扒代码，耗时且易错。
4. **报告不可流转**：结论散落在 IM/口头，缺少标准化的、可归档可导出的分析报告。
5. **资产不直观**：出问题时，难以快速定位"本次到底替换了哪些 class / 哪些 jar"。

### 1.3 建设目标

| 目标 | 说明 |
| --- | --- |
| G1 全量差异 | 自动解包、分层展开、生成"全量差异文件树"，覆盖新增/删除/修改/未变 |
| G2 可读化 | 对 `.class` 内置反编译，转为近似 Java 源码做内容比对（对标 BCompare 的双栏对照） |
| G3 智能化 | 借助大模型（分两阶段）分析差异改动内容，输出风险等级、影响范围、测试要点 |
| G4 可流转 | 一键导出 Markdown 分析报告；导出差异 class / 差异 jar / 反编译源码包供参考 |
| G5 易用性 | 打包为 exe，双击即用；所有配置（尤其 AI 服务）在 UI 上即可完成，无需手改配置文件 |
| G6 合规性 | 支持本地/私有化模型，代码可不出机；提供审计日志（金融场景强制） |

---

## 2. 术语与定义

| 术语 | 定义 |
| --- | --- |
| 老包 (Base) | 生产环境当前在用的版本 war/jar |
| 新包 (Target) | 产品部新下发的版本 war/jar |
| 差异文件树 | 以目录树形式展示两包之间所有文件的变更状态 |
| 反编译 | 将 `.class` 字节码还原为近似 Java 源码，便于阅读与文本对比 |
| 内容比对 | 对同一文件（文本或反编译源码）做行级 diff，标记增/删/改 |
| 影响范围 | 本次改动涉及的模块、功能、对外接口、数据表、依赖组件等 |
| 差异资产 | 本次变更涉及的 class 文件、jar 包、反编译源码等可单独提取的文件集合 |
| BCompare 风格 | 左(old)/右(new) 双栏、树形导航、颜色标记差异的对照交互 |
| 内部业务码 | 属于本系统的业务 class（按"内部包前缀"配置识别，如 `com.xxx`），是差异分析的主视角 |
| 第三方依赖 | `WEB-INF/lib` 中来自第三方的 jar，默认仅比对 jar 级变化 |

---

## 3. 用户与角色

| 角色 | 关注点 | 典型操作 |
| --- | --- | --- |
| 实施/运维 | 升级前评估、出差异资产 | 选两包 → 比对 → 看差异树 → 导出差异 jar/class → 升级 |
| 测试 | 测试要点、回归范围 | 看 AI 生成的测试要点与影响范围 → 编写用例 |
| 研发 | 改动意图确认、风险复核 | 看反编译源码 diff、AI 风险分析 → 复核/答疑 |
| 技术负责人 | 整体风险与影响总结 | 看报告风险汇总 → 决策是否升级 |

---

## 4. 总体功能架构

```
┌──────────────────────────────────────────────────────────────────┐
│                       UI 层 (JavaFX 桌面端)                        │
│  工具栏 │ 差异文件树(左) │ 双栏内容对照(中) │ AI分析面板(右/下)     │
├──────────────────────────────────────────────────────────────────┤
│                          业务层                                    │
│ ①包接入与版本管理 ②包解析引擎(分层) ③文件级差异比对 ④反编译引擎  │
│ ⑤内容比对(diff)     ⑥大模型智能分析(两阶段) ⑦报告生成 ⑧资产导出   │
├──────────────────────────────────────────────────────────────────┤
│                          能力层                                    │
│ 解压(ZIP) │ 哈希(SHA-256) │ 反编译器(CFR/Procyon/Quiltflower)      │
│ 文本diff(java-diff-utils/JGit) │ HTTP客户端(LLM,支持代理) │ 加解密  │
├──────────────────────────────────────────────────────────────────┤
│                      配置层 (UI 配置中心)                           │
│ AI服务配置(含代理/审计) │ 解析配置(分层/前缀) │ 界面/主题 │ 脱敏   │
│ 存储(本地加密)                                                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 5. 功能需求详述

### 5.1 包接入与版本管理

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR1.1 | 选择老包 | 文件选择对话框，支持 `.war`/`.jar`，记录路径与"版本标识" | P0 |
| FR1.2 | 选择新包 | 同上，标记为"新下发版本" | P0 |
| FR1.3 | 自动识别类型 | 依据扩展名与内部 manifest 判断是 war / 普通 jar / Spring Boot fat jar，采用对应**路径映射策略**（见 §5.2 表） | P0 |
| FR1.4 | 解析前校验 | 校验文件存在、可读、ZIP 完整性（防损坏包），给出明确错误提示 | P0 |
| FR1.5 | 历史记录 | 保存最近比对过的包路径与版本标识，可一键复用 | P2 |
| FR1.6 | 批量比对 | 支持一次选中多个新包分别与同一老包比对（可选，首版可不实现） | P3 |
| FR1.7 | **版本自动提取** | 解析 `META-INF/MANIFEST.MF` 的 `Implementation-Version`、以及 `META-INF/maven/*/pom.properties` 的 `version`，自动回填"版本标识"；允许用户覆盖手动填写 | P1 |

### 5.2 包解析引擎

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR2.1 | 解压 | 以流式方式解压 war/jar（ZIP），不依赖外部解压软件 | P0 |
| FR2.2 | **分层粒度解析** | 不再使用单一"递归展开"开关，改用 §5.2.1 的分层策略（L0 包级默认展示、L1 内部业务码主视图、L2 第三方折叠可下钻）；"递归展开"概念由分层策略取代 | P0 |
| FR2.3 | 文件分类 | 按扩展名归类为：class / jar / 配置(xml,properties,yml,yaml,json,conf) / 静态(jsp,html,js,css,图片,字体) / 其他 | P0 |
| FR2.4 | 元数据提取 | 记录每个文件的相对路径、大小、SHA-256、最后修改时间、所属包、所属来源命名空间 | P0 |
| FR2.5 | 增量/分批 | 大包（>500MB 或文件数>阈值）支持分批解析，避免内存溢出 | P1 |
| FR2.6 | 临时目录管理 | 解压产物落在本地临时目录，比对结束/退出时按策略清理，可配置保留 | P1 |
| FR2.7 | **按包类型的路径映射** | 依据 FR1.3 识别的包类型，映射到对应的 classes/lib 路径（见下表），保证 war 与 fat jar 都被正确解析 | P0 |

**按包类型的路径映射表（FR2.7）**

| 包类型 | classes 路径 | lib 路径 | 备注 |
| --- | --- | --- | --- |
| war | `WEB-INF/classes/` | `WEB-INF/lib/*.jar` | 标准 Java EE war |
| 普通 jar | `(根)/` | 无（或按内嵌情况） | 直接包含 class/资源 |
| Spring Boot fat jar | `BOOT-INF/classes/` | `BOOT-INF/lib/*.jar` | 含 `BOOT-INF/` 与 `org/springframework/boot/loader/` |

#### 5.2.1 差异粒度分层策略（评审点 1 / 3 / 10）

为避免"全展开被第三方淹没"或"不展开看不到类级改动"的两难，**默认采用三层粒度**：

- **L0 包级（默认展示）**：把 war 根、`WEB-INF/classes`、`WEB-INF/lib/*.jar` 作为整体文件比对。快速回答"哪些 jar 被替换、根目录/classes 下哪些文件变了"。
- **L1 内部业务码（主差异视图）**：按"**内部包前缀**"配置（如 `com.xxx`、`cn.xxx`）对命中的内部 jar 递归展开其 class，形成业务代码差异视图。这是用户最关心的改动。
- **L2 第三方依赖（默认折叠）**：对第三方 jar 仅比对 jar 是否变化（增/删/改），**不展开其 class**；用户可手动"下钻"展开某个第三方 jar 看内部 class 差异。

**路径命名空间（避免冲突/塌陷）**：用来源前缀区分同名类，例如：
- `WEB-INF/lib/<jar名>/com/xxx/A.class`（来自某 lib jar）
- `WEB-INF/classes/com/xxx/A.class`（来自 classes）
- fat jar 对应 `BOOT-INF/lib/<jar名>/...` 与 `BOOT-INF/classes/...`

**相关配置项**：内部包前缀（列表，可配）、默认展开层级（L0 / L0+L1 / 全展开）、是否允许下钻第三方。

**与 FR3.6 的关系（评审点 10）**：原 FR3.6「只看业务类」由 **L1 内部业务码视图** 统一承载——L1 视图即"仅展示内部包前缀匹配的业务 class/配置"。FR2.2（分层）决定"解析到哪一层"，FR3.4（过滤/搜索）叠加在视图之上做二次筛选，二者不冲突。

### 5.3 文件级差异比对

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR3.1 | 全量差异计算 | 基于相对路径集合求差：新增 / 删除 / 修改(hash 不同) / 未变 | P0 |
| FR3.2 | 差异文件树 | 以目录树展示，节点图标+颜色区分状态（新增=蓝/绿、删除=红、修改=黄、未变=灰） | P0 |
| FR3.3 | 差异统计 | 顶部汇总：新增 N、删除 M、修改 K、未变 L、涉及 jar 包 X 个（区分内部/第三方） | P0 |
| FR3.4 | 过滤与搜索 | 按状态过滤、按文件名/路径搜索、按文件类型过滤、按来源层级（L0/L1/L2）过滤 | P0 |
| FR3.5 | 忽略规则 | 可配置忽略路径/文件名正则（如 `META-INF/`、`*.MF`、构建时间戳文件），减少噪音 | P1 |
| FR3.6 | 业务码视图 | 由 §5.2.1 的 L1 视图承担：一键只看"内部业务 class / 配置"差异，屏蔽第三方依赖噪音 | P0 |

### 5.4 class 反编译与内容比对

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR4.1 | 内置反编译器 | 集成 CFR / Procyon / Quiltflower 之一（默认 CFR，可在设置中切换） | P0 |
| FR4.2 | 反编译触发 | 对"修改 / 新增 / **删除**"的 class 反编译：修改类反编译 old+new 两侧；新增类反编译 new 侧；**删除类仅反编译 old 侧**（新包已无该类） | P0 |
| FR4.3 | 源码内容比对 | 修改类：old/new 反编译源码双栏 diff，高亮增/删/改行（BCompare 风格）。**新增类仅右栏（新包）**；**删除类仅左栏（老包）**，并标记"潜在破坏性变更（对外 API 移除）" | P0 |
| FR4.4 | 文本文件直比 | 对配置/jsp/js/css 等文本文件直接做内容 diff（无需反编译） | P0 |
| FR4.5 | 反编译缓存 | 同一 class 不重复反编译，结果缓存到临时目录 | P2 |
| FR4.6 | 反编译失败兜底 | 个别 class 反编译失败时不中断，标记"反编译失败"，仍按二进制 hash 判定为修改 | P1 |
| FR4.7 | **删除类专项汇总** | 所有"删除"状态的 class 单独汇总为"潜在破坏性变更清单"，在 AI 分析与报告中重点提示（对外 API/类被移除，可能影响调用方） | P0 |
| FR4.8 | **非文本比对边界** | 图片/字体/二进制等"非文本/其他"类文件：**仅展示状态与大小变化，不做内容 diff**；若大小变化提示"资源可能已更新"。避免对非文本做无意义 diff 或误判 | P0 |

### 5.5 大模型智能分析

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR5.1 | 分析输入编排 | 将"差异清单 + 被修改文件的（摘要/完整）反编译源码 diff + 项目背景"组织为模型输入（遵循 §5.5.1 两阶段） | P0 |
| FR5.2 | 改动意图解读 | 说明"改了什么、为什么改（推断）" | P0 |
| FR5.3 | 风险评级 | 对每个改动给出 高/中/低 风险等级及理由 | P0 |
| FR5.4 | 影响范围分析 | 推断受影响模块、功能、对外接口、数据表/字段、依赖组件 | P0 |
| FR5.5 | 测试要点生成 | 生成测试要点清单：功能点、用例方向、重点回归范围、边界场景 | P0 |
| FR5.6 | 结构化输出 | 模型返回 JSON（文件↔{变更类型,风险,影响,测试要点}），解析后渲染到 UI 与报告 | P0 |
| FR5.7 | 分批与限制 | 遵循 §5.5.1：以"文件"为最小单元，单批上限 N（默认 10，可配），超长文件先摘要再深读，控制 token 与成本 | P1 |
| FR5.8 | 重试与降级 | 单次失败可重试；未配置 AI 时跳过智能分析，仅保留基础比对 | P1 |
| FR5.9 | 整体结论 | 汇总得到"整体风险等级"与"整体影响范围概述" | P0 |
| FR5.10 | 可追问 | 对某个文件/某次分析，支持在面板里向模型追加提问（可选） | P3 |
| FR5.11 | **阶段A 概览（默认）** | 输入 = 差异清单 + 每个改动文件的"摘要 diff"（反编译源码取签名/关键方法/前 N 行，默认 N=120；文本取变更片段）。产出：整体风险、整体影响概述、按模块的测试主题、每个文件的初评风险 | P0 |
| FR5.12 | **阶段B 深读（按需）** | 仅对 高/中风险 文件，或用户在差异树勾选的文件，发送**完整**反编译 diff 做逐文件深度分析；低风险且未勾选的文件沿用阶段A 初评，不重复发完整源码 | P0 |

#### 5.5.1 两阶段分析策略（评审点 2）

为解决"逐文件全量发给 LLM 导致超 token / 烧钱 / 不可用"，AI 分析分为两阶段：

- **阶段A 概览（默认自动执行）**
  - 输入：差异清单（路径 + 状态 + 分类 + 大小）+ 每个改动文件的**摘要 diff**（截断策略：反编译源码取类签名/关键方法/前 N 行，默认 N=120 行；文本文件取变更片段）。
  - 产出：整体风险等级、整体影响范围概述、按模块的测试主题、以及每个改动文件的"初评风险"。
- **阶段B 深读（按需/批量）**
  - 触发：仅对 **高/中风险** 文件，或用户在差异树中**勾选**的文件。
  - 输入：**完整**反编译 diff（不再截断）。
  - 产出：逐文件的改动意图、影响范围、测试要点。
  - 低风险且未勾选的文件：沿用阶段A 初评，不重复发送完整源码。
- **单位与限额**：以"文件"为最小分析单元；单批上限 N（默认 10，可配）；超长文件先摘要再深读；总成本可控、可预估。

### 5.6 测试要点与影响范围呈现

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR6.1 | 单文件卡片 | 选中差异树节点时，右/下方面板展示该文件的：变更类型、风险、影响范围、测试要点（删除类额外标"破坏性变更"） | P0 |
| FR6.2 | 全局汇总 | 独立"影响范围 / 测试要点"视图，跨文件聚合，去重展示受影响模块与测试主题 | P0 |
| FR6.3 | 风险热力 | 按风险等级着色，高风险项置顶提示 | P1 |
| FR6.4 | 破坏性变更清单 | 由 FR4.7 驱动：汇总所有删除类，单独成区，提示"对外 API/类被移除，需确认调用方" | P0 |

### 5.7 报告导出（Markdown）

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR7.1 | 一键导出 MD | 导出 `.md` 报告，含以下章节 | P0 |
| FR7.2 | 报告章节与默认体量 | 默认包含：① 元信息 ② 差异统计 ③ **全量差异文件树（折叠/缩进，含 L0/L1/L2 分层）** ④ **改动明细：默认内嵌 Top-K（默认 K=15，按风险排序）风险文件的完整 diff 片段**；其余改动仅列"路径+状态+风险"，不内嵌完整 diff ⑤ 整体风险与影响范围总结 ⑥ 破坏性变更清单（删除类）⑦ 附录（AI 配置快照、模型原始输出、脱敏后上下文） | P0 |
| FR7.3 | 报告样式 | 标准 GitHub 风格 Markdown，表格+代码块，可被 IM/文档系统直接渲染 | P0 |
| FR7.4 | 内容可裁剪 | 导出时可勾选"是否包含反编译源码 diff 片段""是否包含 AI 原文""Top-K 数量"，控制体积 | P1 |
| FR7.5 | 二次格式 | 可选导出 HTML / PDF（首版可仅 MD） | P3 |

### 5.8 差异资产导出

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR8.1 | 差异 class 导出 | 将"修改/新增"的 `.class` 从新包中按**原目录结构（含命名空间）**提取，打包为 zip，供用户直接替换/提交参考 | P0 |
| FR8.2 | 差异 jar 导出 | 将 `WEB-INF/lib`（或 `BOOT-INF/lib`）中"修改/新增"的 jar 原文件提取（保留原名），打包为 zip | P0 |
| FR8.3 | 反编译源码包 | 将上述 class 的反编译 `.java` 源码一并打包，便于阅读 | P0 |
| FR8.4 | 差异清单文件 | 同时导出 `diff-manifest.csv/json`（全量差异清单，含状态、hash、大小、来源层级） | P1 |
| FR8.5 | 输出位置可配 | 导出目录在设置中指定，默认"桌面/比对产物/时间戳目录" | P1 |

### 5.9 AI 服务配置与 UI 配置中心

> 关键要求：**所有配置在 UI 界面上有入口配置**，不强制手改任何配置文件。AI 服务配置必须清晰、可测试。

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| FR9.1 | AI 厂商预设 | 提供常见厂商预设（OpenAI / 通义千问 / 文心一言 / 智谱 / **本地 Ollama / 私有化模型** / 自定义 OpenAI 兼容），一键填充默认 Base URL | P0 |
| FR9.2 | AI 核心字段 | 可配置：API 类型(兼容协议)、Base URL、API Key、模型名(model)、Temperature、超时(秒)、最大重试次数 | P0 |
| FR9.3 | 连接测试 | "测试连接"按钮：用最小请求验证 Key/URL/模型可用性并提示结果 | P0 |
| FR9.4 | 多预设管理 | 可保存多组 AI 配置（如"生产用/公网""本地用/私有化"），随时切换 | P1 |
| FR9.5 | 解析配置 | UI 配置：内部包前缀、默认展开层级、是否允许下钻第三方、忽略规则正则、大包分批阈值、反编译器选择 | P0 |
| FR9.6 | 导出配置 | UI 配置：默认导出目录、Top-K 数量、是否含源码 diff、是否含 AI 原文 | P1 |
| FR9.7 | 界面配置 | 主题（浅色/深色）、字体大小、差异颜色方案（可对标 BCompare） | P2 |
| FR9.8 | 脱敏规则 | 配置发送给模型的文本中需屏蔽的敏感串（如密钥、内网域名正则） | P1 |
| FR9.9 | 本地安全存储 | API Key 等敏感信息在本地加密存储（如基于机器特征的对称加密），**不落明文**，不随报告导出 | P0 |
| FR9.10 | 离线可用 | 未配置 AI 时，基础比对/导出照常工作，仅智能分析不可用并明确提示 | P0 |
| FR9.11 | **代理配置** | 访问 LLM 需经企业代理时，可配置 HTTP/HTTPS 代理地址、端口、是否需要认证；连接测试走代理 | P1 |
| FR9.12 | **本地模型优先提示** | 金融环境下默认推荐/高亮"本地 Ollama / 私有化模型"预设，并在 UI 标注"选用本地模型时反编译代码不出机" | P1 |
| FR9.13 | **审计日志** | 记录每次比对行为：操作人（本机用户）、时间、老/新包路径与版本、是否外发给公网模型、模型厂商。日志本地存储，可导出，满足金融审计留痕 | P0 |

#### 5.9.1 金融合规与审计（评审点 4）

票据系统属金融系统，代码外发受合规约束，本工具将"合规"作为一等需求：

- **代码可不出机**：默认鼓励使用**本地 / 私有化模型**（FR9.1/9.12）。当配置为本地模型时，反编译源码**全程不离开本机**，不产生任何外网请求（验收见 AC9）。
- **公网模型最小化外发**：当必须使用公网模型时，仅发送"脱敏后的 diff/源码文本片段"（FR9.8），绝不发送完整包或无关文件；API Key 本地加密（FR9.9）。
- **审计留痕**：每次比对生成审计日志（FR9.13），记录是否外发、发往哪个厂商，供合规核查。
- **明确标注 AI 推断**：报告与 UI 中对 AI 产出统一标注"AI 推断，需人工复核"，避免误当作结论。

### 5.10 UI 布局与线框（评审点 9）

三区联动布局，避免"底部/侧栏"含糊：

```
┌──────────────────────────────────────────────────────────────────────┐
│  工具栏：[选择老包] [选择新包] [开始比对] [AI分析] [导出报告] [导出资产] [设置] │
├───────────────┬──────────────────────────┬──────────────────────────┤
│ 差异文件树(左) │  双栏内容对照(中)          │  AI 分析面板(右)          │
│ ─────────────  │  ──────────────────────  │  Tab1: 单文件卡片         │
│ ▸ WEB-INF      │  左(old)        │ 右(new) │   变更类型/风险/影响/测试 │
│   ▸ classes    │  ┌────────┐ ┌────────┐  │  Tab2: 全局汇总(影响/测试) │
│     ●Bill.class│  │ old源码 │ │ new源码 │  │  Tab3: 破坏性变更(删除类) │
│   ▸ lib        │  │  diff   │ │  diff   │  │  Tab4: 审计/配置快照      │
│     ~xxx.jar   │  └────────┘ └────────┘  │                            │
│ ▸ BOOT-INF     │  （颜色高亮增/删/改行）   │  [逐文件展开/收起]        │
├───────────────┴──────────────────────────┴──────────────────────────┤
│  状态栏：进度/统计（新增N 删除M 修改K 未变L）│ 日志                      │
└──────────────────────────────────────────────────────────────────────┘
```

- **左**：差异文件树（§5.3），支持过滤/搜索/层级切换。
- **中**：双栏内容对照（§5.4），class 显示反编译源码 diff，文本显示原文 diff，非文本仅状态。
- **右**：AI 分析面板，单文件卡片 / 全局汇总 / 破坏性变更 / 审计 四个 Tab。
- 三区随"选中树节点"联动；AI 分析可整体触发（阶段A）或针对勾选文件（阶段B）。

---

## 6. 非功能性需求

| 类别 | 要求 |
| --- | --- |
| 部署形态 | 打包为 **exe**，双击即可运行；内嵌 JRE，用户无需预装 Java |
| 运行环境 | Windows 10 / 11（64 位）；内存建议 ≥ 8GB，大包场景 ≥ 16GB |
| 性能 | 单包 ≤ 200MB 时：解压+哈希+差异树 < 30s；内容 diff 秒级；AI 分析按两阶段进行 |
| 内存 | 流式处理，避免整包载入；大包可分批，防止 OOM |
| 兼容性 | 支持 war、普通 jar、Spring Boot fat jar（按 §5.2 路径映射）；容忍损坏包并提示 |
| 安全与合规 | 包不上传第三方；公网模型仅发脱敏后 diff 文本；Key 本地加密；**本地模型时代码不出机（§5.9.1）** |
| 审计 | 每次比对生成审计日志（FR9.13），记录是否外发及目标厂商 |
| 可观测 | 比对过程有进度条与日志；失败有可定位的错误信息 |
| 国际化 | 首版中文 UI |

---

## 7. 技术选型建议

| 维度 | 推荐方案 | 说明 |
| --- | --- | --- |
| 语言/运行时 | Java 17 + 内嵌 JRE | 与待分析目标同生态，反编译库为 Java 原生，集成最顺 |
| 桌面框架 | JavaFX 21 | 跨控件丰富（TreeView、双栏 diff、Tab、Dialog），适合做 BCompare 风格 UI |
| 打包 | `jpackage` / `launch4j` | 生成原生 exe + 安装包，双击即用，内嵌 JRE |
| 解压/哈希 | JDK `java.util.zip` + `MessageDigest`(SHA-256) | 无需额外依赖 |
| 反编译 | **CFR**（默认）/ Procyon / Quiltflower | 纯 Java，易内嵌；CFR 对现代 Java 支持好 |
| 文本 diff | `java-diff-utils` 或 JGit `DiffFormatter` | 行级 diff，输出统一/上下文格式 |
| HTTP 客户端 | OkHttp / JDK HttpClient（支持代理） | 调用 LLM OpenAI 兼容接口，走企业代理 |
| JSON | Jackson | 解析模型结构化输出 |
| 加密存储 | Jasypt / JDK `Cipher`(AES) | 本地加密 API Key |
| 备选方案 | Electron + Node + 子进程调 Java 反编译 | 若坚持 Web 技术栈；但反编译需 Java 子进程，复杂度更高，**不首选** |

> 结论：推荐使用 **JavaFX + 内嵌反编译器 + jpackage 打包** 的纯 Java 桌面方案，最契合"分析 Java 包 + 双击 exe + UI 配置一切"的目标。

---

## 8. 典型使用流程（用例）

```
1) 用户双击 exe 启动工具
2) 设置 → AI 服务：选择厂商预设（金融环境优先选"本地 Ollama/私有化"）→ 填 Base URL / API Key / 模型 → 点"测试连接"通过
3) 主界面：选"老包"(生产版本 war) → 选"新包"(产品部下发 war) → 点"开始比对"
4) 工具按 §5.2.1 分层解析：L0 包级 + L1 内部业务码为主视图，生成差异文件树（新增/删除/修改/未变，颜色标记）
5) 用户在差异树中点开某个被修改的 class → 中栏显示 old/new 反编译源码双栏 diff；删除类显示左栏+破坏性标记
6) 点"AI 智能分析" → 阶段A 概览：整体风险/影响/测试主题 + 各文件初评；阶段B 深读：对高/中风险或勾选文件发完整 diff
7) 查看"整体风险与影响范围"汇总、以及"破坏性变更清单（删除类）"
8) 点"导出报告" → 生成 Markdown 报告（默认含 Top-K 完整 diff + 全量清单）
9) 点"导出差异资产" → 下载 差异 class / 差异 jar / 反编译源码 的 zip 包
10) 将报告与资产包 + 审计日志提交至发版/升级评审流程
```

---

## 9. 产物与数据模型

### 9.1 内部差异记录（内存/临时）

```json
{
  "path": "WEB-INF/lib/bemp-core.jar/com/xxx/BillService.class",
  "namespace": "WEB-INF/lib/bemp-core.jar",
  "type": "class",
  "layer": "L1",
  "status": "MODIFIED",
  "oldSha256": "…", "newSha256": "…",
  "oldSize": 12345, "newSize": 12678,
  "decompiled": true,
  "analysis": {
    "intent": "…", "risk": "MEDIUM",
    "impact": ["模块A","接口B","表C"],
    "testPoints": ["…", "…"]
  }
}
```

### 9.2 导出产物

| 产物 | 形态 | 用途 |
| --- | --- | --- |
| 分析报告 | `差异分析报告-<老版本>-<新版本>-<时间戳>.md` | 流转/评审/归档 |
| 差异 class 包 | `diff-classes-<时间戳>.zip`（原目录结构，含命名空间） | 直接替换/提交参考 |
| 差异 jar 包 | `diff-jars-<时间戳>.zip` | 升级时定位被换的依赖 |
| 反编译源码包 | `decompiled-sources-<时间戳>.zip` | 阅读改动 |
| 差异清单 | `diff-manifest.json / .csv` | 程序化处理/审计 |
| 审计日志 | `audit-<时间戳>.log/.csv` | 合规留痕 |

---

## 10. 验收标准

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC1 | exe 可运行 | 在干净 Win10/11 上双击 exe 启动，无需安装 JDK |
| AC2 | 全量差异 | 选两个真实 war 包，差异树覆盖所有新增/删除/修改文件，统计正确（含内部/第三方区分） |
| AC3 | 反编译可读 | 被修改 class 能反编译并双栏 diff，关键改动行高亮；删除类左栏+破坏性标记 |
| AC4 | AI 分析可用 | 配置 AI 后，两阶段分析可产出风险/影响/测试要点并渲染；未配置时有明确提示且不崩溃 |
| AC5 | 报告导出 | 导出的 MD 在常见 Markdown 渲染器中结构正确；默认含 Top-K 完整 diff + 全量清单 |
| AC6 | 资产导出 | 导出的差异 class/jar 包结构正确（含命名空间）、可解压、文件完整 |
| AC7 | 配置全在 UI | 所有 AI/解析/导出/界面/代理/审计配置均可经 UI 完成，无必须手改的配置 |
| AC8 | 性能 | 200MB 内单包比对 < 30s 出差异树 |
| AC9 | 合规与审计 | 使用本地/私有化模型时，无网络外发请求（抓包验证）；审计日志记录比对行为与外发标记 |

---

## 11. 风险与排期（建议）

| 风险 | 说明 | 缓解 |
| --- | --- | --- |
| 反编译失真 | 混淆/高版本语法可能还原不全 | 多反编译器可切换；失败时按二进制判定为修改并提示 |
| 大包性能 | 超大 war 内存/耗时 | 流式+分批；大包阈值可配 |
| 模型幻觉 | AI 对影响范围推断可能不准 | 明确标注"AI 推断，需人工复核"；保留原始 diff 供核对 |
| 敏感信息外发 | 源码含内网信息 | 脱敏规则 + 仅发 diff 文本 + Key 本地加密；优先本地模型 |
| 第三方噪音 | 全展开被第三方 class 淹没 | 分层策略（L1 主视图 + L2 折叠）+ 内部包前缀过滤 |
| AI 成本失控 | 全量发完整源码超 token/费用 | 两阶段分析（概览摘要→按需深读）+ 单批上限 + Top-K |

> 建议排期：P0 功能（解析分层+差异+反编译+两阶段AI分析+MD报告+exe打包+UI配置+合规审计）为 M1；P1 为 M2；P2/P3 为后续迭代。

---

## 附录 A：差异状态颜色规范（对标 BCompare）

| 状态 | 颜色 | 含义 |
| --- | --- | --- |
| 新增 ADDED | 蓝/绿 | 仅新包存在 |
| 删除 DELETED | 红 | 仅老包存在（潜在破坏性变更） |
| 修改 MODIFIED | 黄/橙 | 两包均存在但内容不同 |
| 未变 UNCHANGED | 灰 | 内容一致（默认可折叠隐藏） |

## 附录 B：首版范围边界（Out of Scope）

- 不实现"在线升级/部署"，仅产出差异资产供人工升级。
- 不支持源码级（git）比对，仅基于构建产物包比对。
- 首版 AI 仅做"分析"，不自动修改代码或自动生成测试用例文件。
- 首版不内置模型权重；本地模型需用户自备（Ollama 等），工具仅做客户端接入。
